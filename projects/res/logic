1. Event новый груз прибыл: ProductIncome
    1) Если грузчик свободен и около точки загрузки:
    Окончание загрузки

    2) Если грузчик свободен, но не рядом:
    Цель: in
    Прибытие в точку за грузом

    3) Свободных грузчиков нет.
    Добавление в очередь загрузки.


2. Event Прибытие в точку за грузом PointAchieved
    Окончание загрузки


3. Event Окончание загрузки WorkerLoaded
    Цель: if (груз необходим в out или ((out & in) is empty & ближайшее событие out c грузом)) {out} else {стелаж}
    Прибытие в точку для разгрузки


4. Event Прибытие в точку разгрузки
    if not prediction
    Окончание разгрузки (Грузчик освободился)
    else
    Ожидание


5. Event Новый заказ на груз:
    Если свободных грузчиков несколько, то определение ближайшего
    1) Если грузчик свободен и около груза:
    Окончание загрузки

    2) Если грузчик свободен, но не рядом:
    Цель: стелаж
    Прибытие в точку за грузом

    3) Свободных грузчиков нет.
    Добавление в очередь для выдачи.

    4) Грузчик с грузом около out
    Окочание разгрузки


6. Event Грузчик освободился
    if (очередь out size > 0) {
        Цель: стелаж
        Прибытие в точку за грузом.
    }
    if (очередь in size > 0) {
        Цель: in
        Прибытие в точку за грузом
    }
    if (ближайшее событие == out) {
        Цель: стелаж
        Прибытие в точку за грузом.
    }
    if (ближайшее событие == in) {
        Цель: in
        Прибытие в точку за грузом.
    }
    Ожидание